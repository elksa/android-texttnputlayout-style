<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Text input layout -->

    <style name="OnboardingTextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxStrokeColor">@color/input_blue</item>
        <item name="boxStrokeWidth">2dp</item>
        <!--
        <item name="android:textColor">@color/selector_edittextcolor</item>
        -->
        <item name="errorTextAppearance">@style/ErrorText</item>
        <!--
        <item name="android:textColorHint">@color/input_gray_enabled</item>
        <item name="android:textColorHint">@color/selector_edithintcolor</item>
        -->
    </style>

    <!-- Text input layout second option -->

    <style name="ErrorText" parent="TextAppearance.Design.Error">
        <item name="android:textColor">@color/input_error</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="HintText" parent="TextAppearance.Design.Hint">
        <item name="android:textColor">@android:color/holo_purple</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
